export default class Stat{
    constructor(name, key,min,max,current){
        this.name = name
        this.key = key
        this.min = min
        this.max = max
        this.current = current
    }
}